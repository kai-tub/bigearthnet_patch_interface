name: Test
on:
  push:
    branches: [main]
    paths-ignore:
      - "bigearthnet_patch_interface/**"
  pull_request:
    branches: [main]
    paths-ignore:
      - "bigearthnet_patch_interface/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'
